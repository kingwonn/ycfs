# PENDING_HUMAN — swe-agent 等人签字/回答

> 回答方式:直接在会话里说。当值 AI 会更新对应卡状态、解除阻塞,并在 DIGEST 追加一行。
> loop 不会停下来等这些——它跳过阻塞卡做 ready 卡,每轮在 DIGEST 报告卡点未解除。

## ✅ 已立法(2026-07-07,原话与翻译见 [docs/DECISIONS.md](../docs/DECISIONS.md))

- **Q1 → D-001**:STM32 + X-CUBE-MCSDK 生成 FOC + GCC 全容器化。解锁 F1、G1。
- **Q3 → D-003**:数据可上云,SOTA 效果优先;数据门禁保留「云默认 + 按项目可收紧」。解锁 H1。
- **Q4 → D-004**:全球目标市场(CE+UL/FCC+CCC+62133),合规矩阵按最宽立骨架。**部分未答**:`verified` 签字人/证据形态、真机-HIL 资源(见下方 Q4b)。

## ⏳ 仍等人的问题

### Q9 · 吹风机电机拓扑 — ✅ 已答(2026-07-07):三相(原话"目前是三相的,但是他们单相我也想了解")
- D-001 修订案生效,G2 结案;单相认知储备另出 primer。

### Q11 · MISRA 偏差登记册批准(10 秒事项)
- **等什么**:对 [docs/misra-deviations.md](../docs/misra-deviations.md) 的 9 条规则级偏差(DEV-001~009,全为裸机惯用法/项目风格)签「批准」,或点名要求清零的规则(12.1 已列清零计划)。
- **解锁**:登记册批准列由「待签」转正;认证阶段以此为审计底稿起点。

### Q10 · J1 分区占位设计评审签字(方向门禁,10 秒事项)
- **等什么**:认可 BOOT 32K / APP 472K / PARAMS 4K 分区表与 RDP L1 起步策略(详见 [docs/ota-partition-plan.md](../docs/ota-partition-plan.md);证伪条件已写:永不联网可回收 28K)。
- **解锁**:分区表由「占位」转「已立法」,G4 生成工程按此布局。

### Q9b · 电机参数(G4 参数包的最后输入)
- **等什么**:目标转速、极对数、电机电气参数(相电阻 R / 电感 L / 反电动势常数 Ke,或给实物用 Motor Profiler 实测)。
- **解锁**:G4(Workbench 工程+参数包生成)。

### Q8 · 首席工程师 review 追加的资源/信息项(详见 [docs/chief-review.md](../docs/chief-review.md))
- **等什么**:① 成本/供应链数据源——有无内部优选器件库/成本库/元器件平台账号(解锁 I2);② 自动化台架预算与场地拍板(解锁 J3);③ 若手头已有吹风机单相驱动的老方案资料,直接提供可让 G2 免调研。
- **解锁**:I2、J3;加速 G2。

### Q7 · 执行脊柱组合签字(D-005 提案,完整对比见 [docs/spine-comparison.md](../docs/spine-comparison.md))
- **背景**:立法者对 OpenHands 路线不满意,点名六方重审(2026-07-07 六路并行调研完成)。
- **提案**:执行脊柱 = **opencode server(主,MIT/183k★/模型无关/真 C-S)+ 可插拔适配层**(保留切换 Claude Agent SDK/Codex);沙箱 = 自建 Docker(F1 已验证)为基线;前端 = Vercel AI SDK 7 + AI Elements(InlineCitation 溯源)+ 抄 coding-agent-template 骨架;Hermes 排除;AWS 取素材(IDT 测试套件/Kiro spec 流程)不取脊柱。
- **等什么**:签字采纳 / 指定调整(如「主选换 Claude Agent SDK」)。
- **解锁**:A1 执行脊柱卡重写并开工。
- **等什么**:一份真实自测表(行是什么、真值从哪来、布尔还是数值容差)。
- **解锁**:B2 的 L0 表形态定稿。骨架先按推荐路径搭(60730 表+认证 STL+异模判题),样例到达触发前提重审。

### Q4b · `verified` 签字人与真机/HIL 资源(Q4 的未答部分)
- **等什么**:谁按什么证据(真机报告/示波器截图/签字表)把卡签成 `verified`;现有真机资源(工作台+电机 / 测功机 / 实时 HIL / 暂无)。
- **解锁**:E2 签字产物 schema 的必填字段与 L-real 可脚本化程度。

### 以下为原始访谈存档(Q5/Q6 未答,Q1/Q3/Q4 已立法归档)

### Q1 · 目标 MCU/电机控制平台定了吗(STM32 / TI C2000 / Infineon iMOTION / Renesas RA / 国产 GD32-AT32 / 待选)?更关键:电机控制那部分固件,你们期望是厂商工具生成/参数化(MCSDK workbench 生成、iMOTION 固定引擎只配脚本),还是要 agent 手写/改写 FOC 算法本身?这些 MCU 的编译工具链是开源 GCC 还是专有 IAR/Keil,能否在无头 CI/容器里合法运行?
- **为何改架构**:这一问同时锁定三件事:agent 真正的 coding 表面积(手写算法 vs 核对生成物/配置)、验证器审的对象、以及沙箱是云还是气隙。若电机环是生成/固定的,整个 CI 围绕『核对配置与生成物 diff』而非『审算法逻辑』搭;若工具链专有且不能容器化,『可编译通过』门禁要么建 license server+本地沙箱要么无法自动化。开源 SWE-agent 生态对此零覆盖,是整条工具链的地基。
- **选项与后果**:
  - **STM32+X-CUBE-MCSDK 生成 FOC,GCC 可全容器化** → .ioc/生成物 diff 核对成为机器可验证主线,CubeMX+STL+Renode 路径最顺;agent 产出=workbench 配置+应用层胶水+保护状态机。
  - **Infineon iMOTION 固定引擎(只参数化/脚本)** → 电机 coding 表面积几乎清零,CI 退化为核对参数/脚本+集成测试;卡结构重心移到应用层/BMS/合规自检。
  - **手写 FOC / 国产 MCU 无等价 .ioc** → CI 必须审控制算法逻辑,机器可验证引脚/时钟表主线需另起实现;需多一层带溯源的选型权衡卡。
  - **专有 IAR/Keil 且只能在特定授权机器运行** → 沙箱强制本地 license server+气隙,云 runner(E2B/Modal)出局,CI 层可行性与部署形态被改写。
- **建议(AI 提案,待人立法)**:优先争取 STM32+MCSDK+GCC 全容器化路径,机器可验证性最强、开源栈最完整;若客户已锁国产/专有工具链,则第一批卡先做不依赖工具链的知识库溯源与静态配置核对,把编译门禁标为『阻塞于 license 方案』而非假装绿。

### Q2 · 能给一个老项目里真实的『表格自测』样例吗?那张表行是什么(自检项/寄存器/规格需求?)、真值从哪来(IEC 60730 标准条款 / MCU 规格书 / 器件规格书 / 人工基线?)、判定是布尔还是数值容差?尤其:自检表的期望值(真值列)是 AI 从设计推理里生成,还是从规格书/标准/认证库独立推导?出题/判题是否用与设计 agent 不同的模型?
- **为何改架构**:这是验证器的核心产物形态,决定 L0 真值表形态、真值锚出生证来源、门禁断言怎么写。更是 L4『被测不能自证』的核心落点:若期望值与被测配置同源(同一模型/同一中间量),自检就是自等式,守护噪声——这是最隐蔽、最易被『框架已能打分』掩盖的污染入口(README 亲身踩过)。研究无法从外部推断这个项目内部定义。
- **选项与后果**:
  - **IEC 60730 Annex H 自检覆盖表,黄金表来自标准/认证 STL,独立进程判题** → 最合规,真值有出生证且天然被测不能自证;需建带模型分离与独立真值推导的出题/判题管道。
  - **寄存器/外设配置 vs MCU 规格书核对 + 需求-测试 RTM** → 用 pdfplumber/Camelot 确定性解析+Doorstop RTM 落地,L1 引用解析核验成为主护栏。
  - **AI 生成期望值,异模独立核对** → 半自证,必须加确定性锚(规格书/标准)兜底,否则退化为自等式。
  - **AI 自出自判(纯自等式)** → 必须重构——这守护零信息,是 YCFS 最优先要封死的污染路径。
- **建议(AI 提案,待人立法)**:把黄金表锚到 IEC 60730 标准条款+厂商认证 STL,真值列由独立进程/异模推导,与被测生成物物理隔离;明确拒绝 AI 自出自判,并用『字节对比期望列与被测生成产物是否同源』作为常备证伪探针。

### Q3 · 客户 RFP、原理图、器件/MCU 规格书、老项目资料的机密等级如何?是否绝对不能离开本地送往云端 LLM API 或云沙箱?团队能否/愿意用本地自托管开源模型?
- **为何改架构**:若禁止数据出域,则排除 Claude Agent SDK 云模型、E2B/Modal 云沙箱、Langfuse Cloud、LlamaParse,强制全栈自托管+本地开源模型,直接锁死执行脊柱可选集、观测选型与知识库摄取栈——这是 YCFS『数据门禁』在部署层的落地,也是一次巨大且难回退的架构分叉。
- **选项与后果**:
  - **可用云 API(签 DPA 即可)** → 脊柱/观测/RAG 可选云托管件,交付最快;数据门禁仍需 denylist 具名确认。
  - **混合:代码本地、非敏感可上云** → 需明确红线切分与路由层,PreToolUse hook 拦截敏感文档外传。
  - **严格气隙、全本地自托管+开源模型** → 排除一批云件,RAG 走本地 PyMuPDF/pdfplumber,脊柱倾向 OpenHands/mini 自托管,观测走自建 append-only sink;L5 异模判官需第二个本地模型供应。
- **建议(AI 提案,待人立法)**:默认按『严格气隙可行』设计(数据门禁 denylist+本地模型+自托管观测),把云能力做成可插拔增强而非依赖;这样无论答案如何都不推倒重来,且天然满足最严数据红线。

### Q4 · 这些产品要过哪些市场认证(CE / UKCA / FCC / UL / CCC)?功能安全等级是 60730 Class B 还是更高?『verified』这个终态由谁按什么证据签字(真机报告?示波器截图?),这位持证功能安全/认证工程师会不会在环参与评审?现有真机资源是什么——仅工作台+电机、测功机、还是 Typhoon/dSPACE 实时 HIL?
- **为何改架构**:认证目标决定合规矩阵覆盖的标准编号与是否强制引入商业工具链/认证 STL(门槛只紧不松下开源覆盖不足不能放宽);签字人是否在环决定安全卡能否结案还是永久卡 PENDING_HUMAN;HIL 资源决定 L-real 哪些能脚本化回归、『门禁全绿→verified』那条结构缺失边如何重建。没有明确真机签字产物,要求#4 无法落成状态机约束。
- **选项与后果**:
  - **仅 CE(EN 55014+IEC/EN 60335/60730 Class B),有专用 HIL+固定签字模板** → L3 可半自动、verified 有硬证据;合规矩阵范围最窄,开源+认证 STL 组合够用。
  - **CE+FCC+UL 甚至 CCC/全球,Class B** → 合规矩阵最宽,需并入美标/中标编号,认证审计强制商业 MISRA 工具与认证编译器,架构须预留认证证据管道。
  - **只有手工样机测试,工程师口头/邮件确认** → 必须把签字产物结构化为 schema 并回流审计链,否则审计链在最高仲裁处断裂。
  - **暂无真机流程/无持证工程师在环** → 所有产出止于『待仿真/待真机』级信心,安全卡永久 PENDING_HUMAN,门禁设计再严也落不了地——需先补人核角色。
- **建议(AI 提案,待人立法)**:先按 CE+Class B 立合规矩阵骨架(Doorstop RTM+60730 覆盖表),把认证级证据管道做成可开关的 direction 门禁;无论 HIL 有无,都先把真机签字产物结构化为一等 schema 并作为 verified 的唯一真值锚。

### Q5 · 会提前给的『老项目资料』具体是什么形态——可编译的 git 仓+CubeMX 工程+原理图源文件,还是一堆 PDF/扫描件?其中哪些你们愿意背书为『规范性真值』,哪些只是『仅供参考、可能有坑』?老项目的法律权属与许可证(客户独占?是否含 GPL/受限第三方代码)如何?
- **为何改架构**:决定老资料是能做机器级交叉校验的结构化知识库,还是只能语义检索的参考堆;也决定 L0 真值锚来源。若不肯背书任何一份为真值,真值锚只能是规格书+人接受基线,老代码必须全部隔离成条件性参考,避免旧 bug 被当真值复利传播;许可证若含 GPL 污染却被复用进新客户交付=法律+IP 风险。
- **选项与后果**:
  - **结构化 git+.ioc+Altium,客户独占可复用** → 可做引脚/BOM/固件一致性机器交叉校验;仍须带勘误标签,溯源终点落规格书页而非『旧项目用过』。
  - **半结构化 PDF 为主,只能语义检索** → 复用价值大打折扣,无法自动对比一致性;老资料仅作 RAG 参考,不进真值锚。
  - **混合,需逐份标注规范性/仅参考** → 知识库须能区分『规范性引用』与『仅供参考』,并对唯一 provenance 为老代码的值硬阻断/标红。
- **建议(AI 提案,待人立法)**:无论形态,一律把老项目默认为『条件性参考』而非真值锚,带出处+已知坏点清单;真值锚只认人已接受的规格书+基线,并对『唯一溯源终点=旧项目用过』的值机器硬阻断,防历史 bug 复利。

### Q6 · 这个前端是给『开发者/立法者』驱动 agent 的单一工作台,还是要同时作为『审计员』的只读证据查看面?两类受众能否共用一个界面?另外:swe-agent 是纯内部工具,还是要白标/产品化卖给吹风机·吸尘器 OEM(甚至多租户 SaaS)?
- **为何改架构**:YCFS『裁判与被测物理分离/被测不能自证』:若同一界面既驱动 agent 又渲染审计裁决,存在自证污染,审计面可能必须是物理分离、append-only、独立身份的第二个应用——决定是一个 app 还是两个,推倒重来成本高。产品化/多租户则直接砍掉带 license 条款的候选(Open WebUI 品牌/Dify 多租户/Lobe 非 OSI/Phoenix ELv2 禁转售),只留 MIT/Apache/BSD 干净底座。
- **选项与后果**:
  - **单一 app,纯内部** → 可用干净原语库自建,审计面作只读分区;但需在服务端强制审计 sink 与 agent runtime 物理隔离防自证。
  - **审计员独立只读证据查看器 + 白标交付单个 OEM** → 审计面拆成物理分离的第二应用(外部 append-only sink),前端底座只能用 MIT/Apache 件。
  - **多租户 SaaS 对外售卖** → license 约束最严,Phoenix/Open WebUI/Dify/Lobe 全部出局,观测/前端全走干净许可,鉴权/RBAC 第一天就要。
- **建议(AI 提案,待人立法)**:按『审计裁决面与 agent 驱动面服务端物理分离、trace 落外部只读 sink』设计,即使初期同一前端也预留分离接口;前端底座只选 MIT/Apache(assistant-ui/AI-SDK+shadcn),不为省鉴权吞下品牌/多租户 license 条款。

---

## 数据门禁 · PII / 非本项目资源 / 不可逆(结构性人审前不可执行)

- **NDA 规格书 / 原理图外传**:任何把客户 NDA 规格书、原理图送往外部 LLM API / 云沙箱的动作,denylist 命中即**硬停**,逐一具名确认,**概括授权(「都允许」)无效**。落地卡:BACKLOG H1(阻塞于 Q3)。
- **老项目资料权属**:老资料若含 GPL/受限第三方代码,复用进新客户交付前须过法务门禁。落地卡:BACKLOG D2(阻塞于 Q5)。

## 对外门禁 · 发送 / 发布 / 交付 / 真机切换(状态机上结构性不存在绕过边)

- **系统方案交付客户**:属对外产出,先 `pending_review`,人审后才交付。
- **真机测试签字 = 最高现实仲裁**:AI 只能把卡推进到「待真机」;`verified` 唯一路径穿过一行人的真机签字。落地卡:BACKLOG C1(ready)+ E2(阻塞于 Q4/Q6)。

---
每条写清:**触发条件、需要人给什么、答案解锁什么**。让签字变成 10 秒的事。