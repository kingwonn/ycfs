# ISR 最坏执行时间预算表(骨架)

> B3 卡产物。真实项目中,每个 ISR 必须有最坏执行时间(WCET)预算与实测值;
> 实测归 L-real(逻辑分析仪/GPIO 翻转法),预算与占比推算在此表机器可查。
> 规则:控制环 ISR 总占比 ≤ 70% CPU;任何 ISR 内禁阻塞调用/禁动态分配(gate resource-budget 腿查符号)。

| ISR | 触发源 | 频率 | WCET 预算 (µs) | 实测 (µs) | 占比 | 证据 |
|---|---|---|---|---|---|---|
| (示例) ADC 注入转换完成 | ADC1 JEOC | 与 PWM 同步 | 待定 | 待真机 | — | 待真机 |
| (示例) SysTick | 1 kHz | 1 kHz | 待定 | 待真机 | — | 待真机 |

- 频率与预算的真值来源:时钟树配置(.ioc)+ 规格书;禁止拍脑袋(L1 溯源)。
- 本表为骨架:电机环 ISR 结构待 G1/G2 领域包与 MCSDK 生成物确定后填充。
