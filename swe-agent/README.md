# swe-agent · 嵌入式固件开发助手

> **YCFS「言出法随」方法论的一个真实实例。**
> 面向吹风机 / 吸尘器类产品(对标 Dyson 级),从 RFP 到「可编译 + 表格自测」的固件,最后由人上真机裁决。

这是 [YCFS 方法论](../README.md)在**嵌入式固件工程**领域的落地:人立法(说 RFP 意图、守 NDA 数据红线、批对外交付与真机签字)、AI 执行(消化规格书 → 出方案 → 写码 → 跑门禁)、验证器裁决(分层 L0–L5)、**现实仲裁(人的真机测试是最高、不可绕过的裁决者)**。

本目录目前处于 **v0 脚手架**阶段:调研与架构已完成,决策无关的骨架已落地,**6 个「答案会改变架构」的问题待人回答**([GATES/PENDING_HUMAN.md](GATES/PENDING_HUMAN.md))。

---

## 一、它是什么

一个 **BS 架构**的固件开发助手,把两类性质根本不同的交付物分开对待:

| 交付物 | 性质 | 靠什么结案 |
|---|---|---|
| **系统方案**(RFP → 软硬件方案) | 工程判断密集 | 人门禁 + **每个数字逐值溯源到规格书某页** |
| **固件代码**(规格书 → 可编译 + 表格自测) | 机器可验证 | 确定性检查器(编译 / 静态分析 / 单测 / 仿真 / 自检表) |

它复用成熟开源件做**执行核心**(OpenHands SDK / SWE-ReX)、**观测底座**(OTel + Langfuse)、**前端原语**(assistant-ui / AI SDK),在其之上自建一层**薄「YCFS 治理层」**——这层是护城河,也是任何现成 agent/eval 框架都不具备的:验证器裁决、被测不能自证、门槛只紧不松、真值有出生证、裁判与被测物理分离、门禁是状态机上结构性不存在的绕过边。

详见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。

## 二、一条铁律钉死整个系统的诚实性

> **编译绿 ≠ 功能对。** 无绳吸尘器的 FOC 闭环、吹风机的堵转跳闸与热管理、声学与 EMC —— 这些在无硬件 CI 里**结构上无法被真实验证**。

因此:**AI 永远只能把工作推进到「待真机」,不能自己宣布「verified」。** 唯一通往 `verified` 终态的路径,必须穿过一行**人的真机签字**。这条边在状态机上被结构性删除(见 [docs/verifier-firmware.md](docs/verifier-firmware.md)),不是靠自觉。

## 三、三层验证器地形(固件域特化)

| 层 | 守什么 | 能否在无硬件 CI 自证 |
|---|---|---|
| **L-static** | 交叉编译通过 · 静态分析/MISRA · host 单元测试 · **IEC 60730 Class B 自检覆盖表** · 寄存器↔规格书核对 | ✅ 可,稳定硬门禁 |
| **L-sim** | Renode 指令集/外设仿真 · 中断/看门狗时序 · 状态机 · 可选 MIL 植物模型 | ⚠️ 部分,**Renode 无电机/逆变器物理模型**(是地图,不是领土) |
| **L-real** | FOC 闭环稳定 · 保护真实跳闸 · 热/声/EMC · 认证签核 | ❌ 必须真机 / 实时 HIL + **人签字** |

「表格自测」锚定在 **IEC 60730-1 Annex H 表 H.11.12.7**(Class B 软件自检):一张权威、可枚举、可机器核对的「组件 × 故障类型 × 检测措施」表(CPU 寄存器 / PC / 时钟 / Flash-CRC / RAM-March / 看门狗 / ADC…),真值来自**标准条款 + 厂商认证自检库**,而非被测固件自己的输出——天然满足「被测不能自证」。

## 四、目录

```
swe-agent/
├── README.md                     # 本文件(项目愿景 + 当前状态)
├── BACKLOG.md                    # 任务卡(唯一计划真源,11 张首批卡)
├── DIGEST.md                     # 每轮一行台账(人的唯一必读)
├── gate.py                       # 本项目门禁运行器(诚实标注 blocked 腿,不伪绿)
├── GATES/
│   └── PENDING_HUMAN.md          # 6 个架构级问题 + 数据/对外门禁
└── docs/
    ├── ARCHITECTURE.md           # 架构:7 组件 + BS 数据流 + YCFS 映射
    ├── verifier-firmware.md      # 固件域分层验证器 L-static/L-sim/L-real + L0–L5
    ├── reuse-landscape.md        # 开源复用短名单(adopt/trial/assess/avoid)
    └── unknown-map.md            # 四象限未知地图(多视角撞车=强信号)
```

## 五、当前状态与下一步

- ✅ 多视角互盲盲点扫描 + 开源生态调研 + 架构合成(5 视角并行)。
- ✅ v0 脚手架:架构/映射/复用/未知四份文档 + BACKLOG(11 卡)+ DIGEST + PENDING_HUMAN + 可跑的 gate。
- ⏳ **等人回答 6 个架构级问题**(见 PENDING_HUMAN)。5 张决策无关的卡(执行脊柱 / gate 运行器 / 待真机状态机 / L1 溯源核验 / OTel 审计)可立即开工;6 张卡阻塞于问题答案。

> 回答方式:直接在会话里说。当值 AI 会把答案落成卡、解除对应阻塞,并在 DIGEST 追加一行。
