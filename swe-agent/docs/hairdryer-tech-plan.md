# 高速吹风机技术方案与软件规划(R10,四路调研合成,事实带溯源)

> 原话:"高速风筒吹风机,技术方案,软件框架搭建,以及FOC和PID等算法规划实现和多项目兼容,便于移植且要到戴森顶级级别,做一个loop调研和规划"
> **前提假设(可证伪)**:按 G2 报告推荐路线 A——三相高速 PMSM + STM32G4 + MCSDK。若 Q9 答复为单相定制/专用芯片,§四 FOC 部分按 B/C 路线重推,§二/三/五大部分保留。
> 小白讲解版见 artifact《高速吹风机,拆给你看》。

## 一、「戴森级」量化对标基线(已核实)

| 指标 | 基线 | 出处 |
|---|---|---|
| 电机转速 | ≥110,000 rpm(V9,13 叶轴流叶轮) | designboom/newatlas |
| 气流 | 原始 13 L/s,Air Multiplier ×3 | Dyson 技术规格 PDF |
| 噪音 | 声功率 ≤77 dB(A);**13 叶把主音调推出可听域**(声学设计,非单纯降 dB) | 同上 |
| 温控 | 出风 4 档 100/80/60/28°C;玻璃珠热敏测温 20–40 次/秒(官方手册 20,现款宣传 40+,两说) | Dyson 手册/官网 |
| 整机 | 1600W · 659g 含线 · 重心近手柄 | 技术规格 PDF |
| 国产对标 | 徕芬 LF03:110k rpm/22m·s⁻¹/¥599(峰岹 FU6812);米家 H501:110k rpm/¥249(凌鸥 LKS32MC038);**参数已追平,差距在声学品质/气流组织/配重** | big-bit/充电头拆解 |

**功率架构关键事实**:整机 1600W 中**电机仅 60–105W**(国产拆解实测),大头是加热丝;电机吃 310V 整流母线。→ 电机驱动是"精密活",加热是"功率活",两者软件上必须**联锁**(§四·3)。

## 二、系统技术方案(硬件构成)

```
市电 AC ──整流──► 310V 母线 ──► 三相逆变(IPM/分立半桥)──► 高速 PMSM(1–2 对极,110k rpm)
        │                         ▲ 相电流采样(三电阻 或 单电阻)
        ├──► TRIAC(过零周波调功)──► 加热丝(双叠/分段,~1400W)
        ├──► 辅助电源(降压)──► MCU(STM32G4)+ HMI(按键/LED 环)
        ├──► 负离子高压模块(约 -1.5~-3kV,1W,开关控制)
        └──► 出风口 NTC(玻璃珠)+ 进风 NTC(温差/前馈可选)
安全兜底(硬件,不经软件):自复位热断路器 + 一次性热熔断 ×2(HD17 实拆 184°C)
```

- 加热驱动定过零周波(burst-fire)而非相角:RFI/谐波小,代价是闪变,受 **IEC 61000-3-3** 约束(来源:AllAboutCircuits/EDABoard 工程共识)。
- **IEC 60335-2-23 新版:保护器件不得自复位**;异常测试含堵进/出风口;新增防头发吸入要求(来源:LISUN/IEC 样张)。

## 三、软件框架(多项目可移植,先例全部已核实)

```
products/hairdryer/          ← 产品层:档位状态机、HMI、产品参数表(每产品一目录)
products/vacuum/             ← 吸尘器共用下面所有层
platform/safety/             ← 安全库:保护状态机(protection.c 已在)、60730 自检、电机-加热联锁
platform/core/               ← 统一运行框架:裸机调度(D-006)、事件、观测埋点
platform/control/            ← 控制算法纯函数:PID/前馈/斜坡(host 可单测)
middleware/mcsdk/            ← ST 生成的电机库(只经 MC API 访问,生成文件不手改)
bsp/<board>/                 ← 板级:引脚/外设映射 const ops 表(Zephyr device model 式)
hal/                         ← 芯片 HAL(CubeMX 生成)
```

- **换板不换逻辑**:板差异收敛进 `bsp/<board>/` 的 const ops 结构体(函数指针表,编译期填充置 ROM)——Zephyr device model 的标准做法;Marlin HAL(十余平台同一上层)、Betaflight(每板一 config.h)同证。
- **换电机不换代码**:MCSDK 把电机身份收敛在两个生成头文件——`pmsm_motor_parameters.h`(R/L/Ke/额定电流)与 `drive_parameters.h`(PI 增益/PWM 频率/限幅)。**移植切面就是这两个文件**,经 Workbench 改参重生成,不手改(ST 社区确认)。
- **多产品布局**:`platform/ + products/` 参照 NASA cFS 软件产品线(任务无关核心+每任务 apps,40+ 任务复用);家电业公开范本未查到,此为合理外推(已标注)。
- **host 可测**:控制与安全逻辑全部纯函数(Grenning 双目标 TDD;Ceedling=Unity+CMock 自动 mock)——protection.c 已示范,gate 已在跑。
- **裸机优先(D-006,立法原话:"这个行业MCU资源受限,可能优先要考虑裸机代码实现,然后在考虑freertos或zephyr等")**:无 RTOS——FOC 环住定时器/ADC ISR(MCSDK 默认形态),应用层 = SysTick 分频的**主循环任务表**(1kHz 速度环任务 / 20–100Hz 温度环任务 / 10Hz HMI 任务,时间片错开);`platform/core` 只留一层薄 OSAL 接缝(延时/临界区/任务注册最小接口),未来上 FreeRTOS/Zephyr 只换接缝实现。这也与国产量产生态一致(8051 核/M0 全裸机)。

## 四、算法规划

### 1. FOC(电机环,MCSDK 承载)

- **链路**:相电流采样 → Clarke/Park(转子坐标系)→ **dq 双 PI 电流环** → 反 Park → SVPWM → 逆变桥。G4 的 CORDIC 硬算 sin/cos(约 5×),FOC 整体提速 ~12%,FMAC 卸载滤波。
- **采样拓扑取舍**:三电阻=窗口宽无盲区,ST 默认电流环带宽上限 9000 rad/s;单电阻=省两路运放/免校准但有盲区需移相重构,带宽上限 4500 rad/s(噪声大再减半)。**建议首版三电阻**(带宽余量给 110k rpm),成本版再评估单电阻。
- **高速参数**:100k rpm @1–2 对极 = 1.67–3.33kHz 电频;规则=每电周期 ≥10 次采样 → **PWM 40–50kHz**(MCSDK 上限 50kHz);FOC 执行率可设 PWM/2(≈20–25kHz)省 CPU。
- **无感启动(最难段)**:传统 STO-PLL 需开环 rev-up 强拖后切观测器;**HSO 无开环段**——探测脉冲定位后直接闭环,ZeST 补零速扭矩(6.3.x 进 Workbench,配套 B-G473E-ZEST1S 板);顺风再启动用 start-on-the-fly。国产通行做法=预定位→I/F 电流限幅斜坡→切双闭环(普冉方案宣称 400ms 到 100k rpm)。**策略:首选 HSO 直接闭环,I/F 斜坡作后备**,以真机启动成功率/时间为证伪判据。
- **弱磁**:若额定点电压余量不足,MCSDK 内置弱磁/MTPA/前馈,待电机参数(Q9)定后评估。
- **待确认**:MCSDK 是否含死区电压补偿模块——公开资料未确证,**读生成代码后定**(诚实空白)。

### 2. PID 环(三环套娃 + 温度环)

| 环 | 执行率 | 带宽量级 | 说明 |
|---|---|---|---|
| dq 电流环(PI) | =FOC 率 20–25kHz | 1000–9000 rad/s(按采样拓扑) | MCSDK 生成,保护功率管 |
| 速度环(PI) | 中频任务 ~1kHz | 电流环的 1/5–1/10(约百 Hz) | 稳 110k rpm;斜坡限幅防启动过流 |
| 出风温度环(PI+**风量前馈**) | 20–100Hz | 秒级 | 档位/风量变化前馈到加热功率,压过冲;**Dyson 是否用前馈无公开证据,此为工程推断** |

温控锚点:**湿发角蛋白 151°C 变性**(MDPI 等文献)→ 出风限温 ≤100°C 档位设计 + 过冲防护;测温 ≥20 次/秒(对标 40)。

### 3. 电机-加热联锁(软件安全核心,60730 Class B 范围)

- 规则:转速/风量低于阈值(含堵转/风道堵塞)→ 加热**先降档后切断**;恢复需冷却确认。专利语义佐证:US4003388A(禁"低风量+高热")、US4260875A(晶闸管随条件切/降热)。
- 实现:纯函数状态机(protection.c 同款纪律),阈值全标 PROVENANCE 待溯源;进 bench 埋 bug 题库(K1 扩容)。

## 五、验证地形(哪些 CI 可自证,哪些必真机)

| 层 | 可验证项 |
|---|---|
| **L-static/CI** | 编译/MISRA/资源预算(已有);温控+联锁纯函数单测;MCSDK 参数头文件与规格书溯源核对;分层依赖检查(products 不得 include 芯片头) |
| **L-sim** | 状态机时序/联锁逻辑(Renode);温度环对一阶热模型的过冲仿真(map, not territory) |
| **L-real(人签字)** | 观测器收敛与 400ms 级启动、堵转/联锁真实触发、**声学**(dB(A)+音调)、温控过冲实测、60335-2-23 异常工况、EMC/闪变 |

## 六、落卡

- **G3 · 软件框架骨架(platform/products 分层)** — ready,电机无关可先行。
- **G4 · FOC 集成与启动策略(MCSDK 参数包)** — blocked-on-human(Q9:电机拓扑/参数/极对数)。
- **G5 · 温控 PI+前馈 与 电机-加热联锁** — ready(逻辑先行,阈值 PLACEHOLDER)。

## 七、开放前提与证伪条件

1. **Q9 未答**:电机三相 ODM vs 单相定制——答单相则 §四·1 换 B/C 路线,§三框架与 §四·2/3 保留。
2. 测温频率 20 vs 40 次/秒两说并存——按 ≥20 设计、40 为目标,真机标定定案。
3. MCSDK 死区补偿存在性未确证——G4 开工首日读生成代码定案。
4. 400ms 到顶速为国产厂商宣称(普冉),ST 侧无官方指标——作为目标而非承诺,真机测得为准。
