# DECISIONS — 方向门禁立法记录(每条带出生证)

> YCFS:真值有出生证——谁、何时、原话、被翻译成什么。方向门禁上人签的字落在这里,卡引用这里。

## D-001 · MCU/电机控制平台:STM32 + X-CUBE-MCSDK 生成 FOC + GCC 全容器化

- **出生证**:立法者=项目发起人;时间=2026-07-07;渠道=会话访谈 Q1。
- **原话**:"Q1 A"(选项 A = STM32+X-CUBE-MCSDK 生成 FOC,GCC 可全容器化)
- **翻译**:目标平台锁定 STM32(电机控制甜点 STM32G4);电机环由 MC Workbench 生成/参数化,**agent 的 coding 表面积 = workbench 配置 + 应用层胶水 + 保护状态机 + 合规自检**;CI 主线 = 核对 `.ioc`/生成物 diff + 应用层代码审;工具链 = arm-none-eabi-gcc,无头容器化合法可行。
- **解锁**:F1(交叉编译门禁,全面 ready)、G1(领域包,ready)。
- **证伪条件**:若后续产品线被证实必须用 STM32 之外的平台(如客户指定国产 MCU),本决策作废,G1/F1 的平台特化部分重做;回退成本=中(gate 骨架、L1 溯源、状态机治理均平台无关,可保留)。
- **⚠ 审查附注(2026-07-07 首席工程师 review,详见 docs/chief-review.md A1)**:X-CUBE-MCSDK 面向三相 PMSM/BLDC;吹风机单相 100k+ RPM 2-FET 拓扑可能不在其覆盖内。在卡 G2 核实完成并经人签字前,本决策视为「**吸尘器线成立、吹风机线待核实**」。这是证伪条件的部分触发,不是撤销。

## D-002 · 「表格自测」真值锚:样例后置,先按推荐路径立骨架

- **出生证**:立法者=项目发起人;时间=2026-07-07;渠道=会话访谈 Q2。
- **原话**:"Q2 会有测试表格以后说"
- **翻译**:人将提供老项目真实自测表样例,**在样例到达前不锁死表形态**。骨架先按推荐路径搭(IEC 60730 Annex H + 厂商认证 STL 为真值锚、独立进程/异模判题、字节同源证伪探针),样例到达后校准。
- **保持阻塞**:B2 仍 `blocked-on-human`(等样例);到达时触发**前提重审**。
- **证伪条件**:若样例揭示的真值锚形态与 60730 表根本不同(如纯器件参数符合性表),B2 的 L0 形态重做;回退成本=低(骨架是表驱动的,换表不换引擎)。

## D-003 · 数据出域:云可用,SOTA 效果优先

- **出生证**:立法者=项目发起人;时间=2026-07-07;渠道=会话访谈 Q3。
- **原话**:"Q3,数据都可以,先追求SOTA效果"
- **翻译**:RFP/规格书/原理图/老项目资料**可以送云端 LLM API 与云沙箱**;模型选型以 SOTA 能力优先(执行脊柱可直接用最强云模型);观测/RAG 可用云托管件。**数据门禁不因此消失**:denylist + 逐一具名确认机制仍要建(H1),只是路由默认=云;若未来接入某个 NDA 更严的客户项目,可按项目切换红线,不推倒架构。
- **解锁**:H1(数据门禁,ready——按「云默认 + 可收紧」实现)。
- **证伪条件**:出现第一个合同明确禁止数据出域的客户项目 → 该项目走本地路由;回退成本=低(路由层设计保留了收紧开关)。

## D-006 · 运行框架:裸机优先,RTOS 后议

- **出生证**:立法者=项目发起人;时间=2026-07-07;渠道=会话(吹风机规划进行中)。
- **原话**:"这个行业MCU资源受限,可能优先要考虑裸机代码实现,然后在考虑freertos或zephyr等"
- **翻译**:运行框架采用**裸机**:FOC 环在定时器/ADC ISR(MCSDK 默认形态),应用层用 SysTick 分频的主循环任务表调度;**不引入 RTOS**。分层设计保留一层薄 OSAL 接缝(延时/临界区/任务注册的最小接口),未来若上 FreeRTOS/Zephyr(如需 BLE/OTA)只换接缝实现,平台层与产品层零改动。
- **佐证**:R1 调研——FOC 环通常裸机 ISR,RTOS 对电机环是次要决策;R10 国产拆解——量产方案用 8051 核(峰岹)/M0(凌鸥),资源紧裸机为业界常态;MCSDK 裸机原生支持。
- **证伪条件**:出现第一个必须并发协议栈的需求(BLE/Matter/OTA 后台下载)且主循环调度不堪重负 → 经方向门禁上 FreeRTOS(先)或 Zephyr(重),OSAL 接缝使回退成本=低。

## D-004 · 认证目标:全球市场

- **出生证**:立法者=项目发起人;时间=2026-07-07;渠道=会话访谈 Q4。
- **原话**:"Q4全球目标市场"
- **翻译**:合规矩阵按**最宽**立骨架:CE(IEC/EN 60335-1 + -2-23 吹风机 / -2-2 吸尘器、EN 55014 EMC、IEC/EN 60730-1 Annex H Class B)+ 美标(UL 60335/UL 859、FCC)+ 中国 CCC + 电池 IEC 62133/UL 2054(吸尘器线)。**后果**:认证级审计证据链是一等公民;正式认证阶段需商业 MISRA 工具(Helix QAC/Polyspace/LDRA)与认证编译器背书——开源静态分析腿(cppcheck/clang-tidy)是日常门禁,**不得声称等价于认证审计**。
- **部分未答(仍在 PENDING_HUMAN)**:`verified` 由谁按什么证据签字、现有真机/HIL 资源——影响 E2 签字 schema 的字段与 L-real 自动化程度。
- **证伪条件**:若商业策略收缩到单一市场(如仅 CE),合规矩阵裁剪即可;回退成本=低(矩阵按标准编号分列,删列不伤骨架)。
