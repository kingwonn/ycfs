# 固件域分层验证器 · L-static / L-sim / L-real + L0–L5

> 把 YCFS 的分层验证器([../../docs/verifier-six-layers.md](../../docs/verifier-six-layers.md))落到嵌入式固件领域。
> 层的职责不变,判据换成固件域的:编译产物、静态分析、单元测试、指令集仿真、IEC 60730 自检表、真机签字。

## 零、一条钉死诚实性的地形线

「工作是否成立」在固件域被一条物理边界劈成三段。**这条边界不是政策,是物理**——它决定了哪些结论 CI 能自己给绿灯,哪些**结构上必须**落到人的真机签字:

| 分层 | 守什么 | 无硬件 CI 能否自证 | 结案方式 |
|---|---|---|---|
| **L-static** | 交叉编译通过 · 静态分析/MISRA · host 单元测试 · **IEC 60730 Class B 自检覆盖表** · 寄存器/外设配置↔规格书核对 · 引脚/时钟/功耗预算表 | ✅ **可,稳定硬门禁** | 确定性检查器,门禁绿 |
| **L-sim** | Renode 指令集/外设仿真 · 初始化序列 · 中断/看门狗喂狗时序 · 状态机时序 · 多节点(MCU+BMS)交互 · 可选 MIL 植物模型 | ⚠️ **部分**。Renode 是数字外设**功能模型**,**不含电机/逆变器物理模型** | 检查器绿,但结果打「map, not territory」标签,**永不代替真机** |
| **L-real** | 传感器less FOC 换相/观测器收敛 · 电流采样与 PWM 死区时序 · 堵转/过流**真实**跳闸 · 热管理 · 声学 · EMC · 认证签核 | ❌ **必须真机 / 实时 HIL** | **人上真机 + 签字**,是 `verified` 唯一真值锚 |

> **编译绿 ≠ 功能对。** 这是固件域相对「报表域」的语义鸿沟:一段 FOC 代码可以 MISRA 全绿、单测全过、Renode 跑通,而在真实 110k RPM 高速电机上 bring-up 直接失败(未建模的齿槽转矩 / 反电动势谐波 / 电感饱和)。**仿真马达是地图,不是领土。** 参数最高只到 L3 旁证。

因此状态机上**结构性删除**「gate 全绿 → verified」这条直达边:唯一通往 `verified` 的路径,穿过一行人的真机签字(见 BACKLOG 卡「『待真机』结构性终态断言」)。

## 一、L0 · 真值编译(带出生证)

把权威来源编译成断言集,每条 = (输入, 期望输出, **溯源**)。固件域的真值锚:

1. **人已接受的规格书条款**(RFP / 产品规格 / 器件规格书 / MCU 规格书某页某表);
2. **IEC 60730-1 Annex H 表 H.11.12.7**(Class B 软件自检,见 §六);
3. **厂商认证自检库**(ST X-CUBE-STL / Renesas / Microchip / Infineon)。

每条真值带**出生证**:标准编号 · 版本 · 条款 · 页 · 谁接受 · 何时。

**铁律:老项目仅条件性参考,绝不作真值锚。** 老代码「能出货」不等于「对」(潜伏 bug 恰好没触发);把历史输出当真值锚 = 与过去的自等式。老代码必须隔离成带勘误标签的参考,溯源终点重锚回规格书页,而非「旧项目用过」。

## 二、L1 · 出口零编造(运行时,每个对外数字)

每个对外的关键值(寄存器配置 / 引脚分配 / 时序参数 / 选型数字),离开系统前必须能溯源回真源。

- **确定性解析,不是 LLM 自证有引用**:用 `pdfplumber`/`Camelot` 抽取被引页/表,**确认声称的数值字面出现在该页**。这封死一条承重红队路径——`correctness ≠ faithfulness`:LLM 会 RAG 出一个错值、再编一个像真的页码;「检查有无引用」会放行,「检查引用是否解析得到该值」才能拦住(参考现实教训:Mata v. Avianca 伪造引用判例)。
- **中英等强**:护栏不能只在一种语言下生效。property-based:中文规格书样本拦截率 = 英文样本拦截率。
- 溯源不到 → **硬阻断**(返回明确错误 + 未溯源项),绝不放行一个「看起来对」的值。

## 三、L2 · 发布前门禁(每次,一条命令 N 腿)

`gate.py` 跑全部单元验收 = N 条并列独立腿。任一腿红 → 退出非零。**任一腿绿不代言其他腿**(MISRA 全绿只保证用了 C 安全子集,不等于功能安全或功能正确)。固件域的腿:

| 腿 | 工具 | 断言形态 |
|---|---|---|
| 交叉编译 | arm-none-eabi-gcc + CMake headless | 产 .elf/.map;size/section 报告解析成断言 |
| 静态分析/MISRA | cppcheck(MISRA addon)+ clang-tidy-misra | 违规数;**抑制项列表只缩不扩** |
| host 单元测试 | Ceedling + Unity + CMock | `v_min`:断言数 ≥ 地板 且 失败=0;gcov 覆盖率 |
| 指令集仿真 | Renode + Robot Framework | 初始化/中断/看门狗时序断言(打 map-not-territory 标签) |
| **60730 自检覆盖表** | 规格符合性对照 | 每项自检 POST/BIST 分类 + 故障类型 + 实现与否,机器可查 |

**门槛只紧不松**:`v_min` 断言下限单调地板、MISRA 抑制项只缩不扩、自检项数不许静默变少。「为了过而调低阈值」就是自欺,写进纪律。

## 四、L3 · 持续在线(夜检)

发布前全绿 ≠ 一直对。夜检定时重跑全 gate,外加固件域特化项:

- **溯源链重解析**:防 errata / 规格 revision 悬空——硅片 errata 或规格书改版会让昨天成立的溯源真值今天失效。溯源必须绑**文档版本 + errata**,而非仅页码。一次 revision 可让一批寄存器配置从对变错而无人知晓。
- **自检项数防静默变少**:检出「测试被跳过/删除」。
- **缺勤告警**:探针靠定时器,定时器停了要有人知道,否则「一直绿」其实是「根本没跑」。

## 五、L4 · 覆盖增长(被测不能自证)

自检表 / 断言集要能自增长,但**不能自我论证**。管道:出题引擎生成候选 → 人审判题目质量 → 机器 0 容差比对判对错 → 双门都过才晋级。

**防自等式的铁律(固件域最优先封死的污染入口):**
- 自检表的**真值列由独立来源(规格书/标准/异模)重新推导**,与被测生成物**物理隔离**。
- 晋级的真值锚**不能是被测系统自己的编译输出**。否则今天的静默 bug 会被晋级成明天的「真值」,以 0 容差永久守护一个错误。
- **常备证伪探针**:字节对比「期望列」与「被测生成产物」是否同源——同源即自等式,守护零信息(呼应 README 亲身踩过的「3000+ 断言 = 字节相同」坑在固件域的翻版)。

## 六、「表格自测」的权威锚:IEC 60730-1 Annex H 表 H.11.12.7

家电控制软件(Class B)的自检在标准里就是**一张可枚举、可机器核对的表**——这正是要求#3「表格自测」最该锚定的对象:

| 组件 | 故障类型 | 可接受检测措施(示例) | 时机 |
|---|---|---|---|
| CPU 寄存器 | stuck-at | 写 0x55/0xAA 回读 | POST + BIST |
| 程序计数器 | stuck-at | 逻辑监控 / 独立时基 | BIST |
| 中断 | 无中断/过频 | 计数与时间窗核对 | BIST |
| 时钟 | 错频 | 独立时基频率监测 | BIST |
| 不变存储器 Flash | 单比特错 | **CRC / 校验和** | POST + BIST |
| 可变存储器 RAM | DC 故障 | **March 测试** | POST + 周期 BIST |
| 寻址 | stuck-at | 上述测试覆盖地址线 | POST/BIST |
| 模拟/ADC | 漂移/卡值 | 已知基准回读 | BIST |
| 独立看门狗 | 失效 | 窗口喂狗 + 复位验证 | 运行时 |

真值来自**标准条款 + 厂商已认证的自检库**,不是被测固件自己的输出——**天然满足「被测不能自证」**。swe-agent 把这张表编码成 L0 规格符合性对照表,「表格自测」门禁 = 60730 自检覆盖表 + 寄存器/外设配置核对表。

## 七、L5 · 语义裁决(异模 · 物理分离)

数字全对但「选型理由空洞 / 方案答非所问 / 张冠李戴」——需要异模裁判。严守:异模(裁判 ≠ 设计 agent ≠ 出题模型)、与设计 agent **物理分离且不共享 RAG 上下文**(否则放行与自己相同盲区的空洞理由)、保守偏置(只能绿改红)、可复现(温度归零)、真人盲标基线前一致率不进对外话术。

## 八、对抗红队:这系统最可能在哪「骗人」

每条附**证伪条件**(什么现象证明它在骗人):

| 骗人路径 | 证伪条件 |
|---|---|
| 「可编译通过」被误当「功能正确」 | 出现一个 CI 全绿但真机 bring-up 失败的案例 → 说明 CI 绿被当成了 verified |
| 仿真马达 ≠ 真实马达,MIL 参数被当验证 | Renode/MIL 跑通的控制参数在真机上跳闸/失稳 |
| LLM 从规格书 RAG 出错值却编出溯源 | 抽查对外值,其页码解析后不含该数值(L1 确定性解析应已拦住) |
| 把老项目历史 bug 当参考真值复刻 | 某值唯一溯源终点 = 「旧项目用过」而非规格书页 |
| 自检表 AI 自出题自判(自等式) | 期望列与被测生成产物字节同源 |
| 中英规格书混用,护栏只在一种语言生效 | 中文样本拦截率 ≠ 英文样本拦截率 |
| 裁判 = 被测同一模型 | 裁判服务与设计 agent 同进程/同模型/共享 RAG |
| 「gate 全绿 → verified」被静默直达 | 存在无真机签字行却处于 verified 的卡 |

## 九、一张自检清单

- [ ] 我的真值有出生证吗?(标准编号 / 条款 / 页 / 谁接受)
- [ ] 每个对外值都能**确定性解析**溯源吗?溯不到会硬阻断还是「看起来对」就放行?
- [ ] gate 每条腿阈值、自检项数、MISRA 抑制项,历史上只紧过没松过吗?
- [ ] 夜检重解析溯源链了吗?errata/revision 悬空会被发现吗?
- [ ] 自检表真值列,是不是被测系统自己的编译输出?字节同源探针跑了吗?
- [ ] 裁判和设计 agent,是同一个模型 / 共享 RAG 吗?
- [ ] 中英护栏拦截率相等吗?
- [ ] 有没有一张 verified 的卡,缺真机签字行?
