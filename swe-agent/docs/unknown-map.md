# 四象限未知地图

> 合成自 5 个互盲视角的盲点扫描。**不同视角撞到同一个未知 = 强信号**(下标注「⚡多视角撞车」)。
> 方法论出处见 [../../docs/finding-your-unknowns.md](../../docs/finding-your-unknowns.md)。

|  | 你意识到 | 你没意识到 |
|---|---|---|
| **你知道** | 已知已知(写进提示词的) | 未知已知(显而易见到不会写、看到能认出) |
| **你不知道** | 已知未知(知道自己没想明白) | 未知未知(完全没考虑过) |

## 已知已知 — 已锁定的地基

- 没有任何现成 agent/eval 框架内建 YCFS 治理不变量(被测不能自证/门槛只紧不松/真值出生证/裁判与被测物理分离/门禁是状态机上不存在的绕过边),必须自建一层薄治理层挂在复用件之上——这是 YCFS 相对通用框架的护城河与核心 IP。
- 执行核心不必重造:OpenHands SDK / mini-SWE-agent / SWE-ReX 等 MIT 件成熟;BS 前端有 assistant-ui/AI-SDK/AG-UI 干净原语;要求#7 在『AI 执行者』这层完全成立。
- IEC 60730 Annex H 表 H.11.12.7 Class B 自检是『表格自测』的权威、可枚举、可机器核对真值锚,厂商已有认证 STL,真值来自标准+库而非被测自证。
- 『可编译通过』可无硬件 headless 自动化(arm-none-eabi-gcc+CMake+Docker),是稳定的 L0/L1 硬门禁;L-static+L-sim(Renode/单测/MIL)是 CI 可自证绿灯的上限。
- 编译绿≠功能对是固件域相对报表域的语义鸿沟:FOC 闭环/换相时序/热/声/EMC/认证签核结构上必须真机,agent 只能到『待真机』,必须结构性删除『gate 全绿→verified』直达边。
- 吹风机(单相高速 2-FET、市电直驱、热主导)与吸尘器(三相 PMSM/BLDC + Li-ion BMS、电池主导)是两套不同电机拓扑与合规面,不能用一套通用『电机 agent』通吃。

## 已知未知 — 已提炼成 PENDING_HUMAN 访谈的问题

- 目标 MCU/厂商平台,以及『厂商 SDK 生成/固定引擎 vs 手写 FOC』的边界在哪——决定 agent 真正 coding 表面积与验证器要审的对象(多视角撞车,强信号)。
- 『表格自测』的确切语义与真值锚来源:IEC 60730 自检覆盖表 / 寄存器-规格书核对 / 需求-测试 RTM / 器件参数符合性表 / 静态 vs 仿真(多视角撞车,强信号)。
- 编译工具链是否专有(IAR/Keil/厂商私有)及能否在无头 CI/容器合法运行——直接决定沙箱是云还是气隙自托管,通用 SWE-agent 生态零覆盖。
- 是否走正式安全认证(60335-2-23/-2-2 + 60730 Class B + 62133 + UL/CCC)及 CI 是否需产认证级审计证据链(认证编译器/认证 STL/MISRA 合规矩阵)。
- NDA 规格书/原理图能否离开本地送云端 LLM/沙箱——决定本地私有模型全栈自托管 vs 云,是巨大架构分叉(多视角撞车)。
- 真机/HIL 资源现状(仅工作台+电机 / 测功机 / Typhoon-dSPACE 实时 HIL / 暂无)——决定 L-real 哪些可脚本化回归、PENDING_HUMAN 队列多重(多视角撞车)。
- 老项目资料形态(可构建 git 仓+.ioc+原理图源 vs 散乱 PDF/扫描件)与法律权属/许可证(客户独占?GPL 污染?)。
- 前端是纯内部工具还是白标/多租户 SaaS——决定 license 可接受集(Open WebUI 品牌/Dify 多租户/Lobe 非 OSI/Phoenix ELv2 禁转售)。
- 吹风机 vs 吸尘器:一个通用 agent 还是两条专用 lane;老项目资料覆盖哪条线、用何拓扑。
- 执行脊柱 build-vs-buy(OpenHands 重·事件溯源 vs mini 轻·易审计 vs Claude SDK 耦合 Anthropic)——决策空间已知但选择本身与工具链/气隙强耦合,待人立法。

## 未知已知 — 显而易见但易被跳过的承重陷阱

- L1 若实现为『检查有无引用』而非『检查引用是否解析得到该值』,LLM 会 RAG 出错值再编像真的页码,溯源护栏沦为剧场(correctness≠faithfulness,已有 Mata v. Avianca、撤稿判例)——多视角识别为承重护栏漏洞。
- 现成 LLM-as-Judge 默认允许裁判指向被测同一模型,YCFS 铁律要求裁判异模、物理分离、不共享 RAG，须自建；同源裁判会放行与自己相同盲区的空洞选型理由。
- 仿真马达≠真实马达:MIL/Renode plant model 只是地图,未建模的齿槽转矩/反电动势谐波/电感饱和会让模型跑通的控制参数真机 bring-up 失败,参数最高只到 L3 旁证。
- 硅片 errata / 规格书 revision 会使昨天成立的溯源真值今天失效,溯源必须绑文档版本+errata 而非仅页码;一次 revision 更新可让一批寄存器配置从对变错而无人知晓。
- 老项目『能出货』不等于『对』(潜伏 bug 恰好没触发),把历史输出当真值锚=与过去的自等式(README 亲身教训),老代码必须隔离成条件性参考并重锚回规格书。
- MISRA/静态分析全绿只保证用了 C 安全子集,不等于功能安全或功能正确;把『MISRA 全绿』当『方案对』是制造虚假信心的路径,各腿绿不得代言其他腿。

## 未知未知 — 盲点扫描逼出来的、否则会踩的坑

- 『表格自测』真值锚若由 AI 自出题、真值列又取自被测自身编译输出,即自等式,守护零信息——README 亲身踩过的『3000+ 断言=字节相同』坑在固件域的翻版(视角2&5 同撞,最强红队信号)。
- 中英规格书混用时护栏可能只在一种语言生效,从中文规格书 RAG 出的错值/伪造引用整条溜过,是被忽视的单语言盲区。
- 审计/裁决呈现面是否必须与 agent runtime 物理分离成第二个 append-only 只读独立应用(否则同屏驱动+裁决=自证污染)——决定是一个 app 还是两个。
- 真机测试证据(录像/示波器/量测/签字表)如何结构化回流进 trajectory/审计链——真机在平台外发生,不接回来审计链在最高仲裁处断裂。
- 跨文档冲突(规格书引脚 vs 原理图连线 vs MCU 复用表打架)谁裁定真值——若无人裁,AI 会自选一个当真值,踩『被测自证』,是隐藏的巨量工作量。
- 电机控制 ISR 的实时预算与 IEC 60730 安全相关段(SIL/Class)划分:哪些代码段必须自检覆盖——划错会漏检安全路径或过度约束非关键代码。
