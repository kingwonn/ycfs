# OTA / 安全启动占位设计(J1,R16)——先锁分区,防后补推倒链接脚本

> 目的:C3 review 指出 Bootloader/OTA/安全启动**后补会推倒 flash 布局与链接脚本**。
> 本占位不实现 OTA,只把「分区约束」定死并让链接脚本立即预留,后续实现只填内容不动骨架。
> 待人评审签字(PENDING_HUMAN);带证伪条件。

## 分区表(STM32G474RE,512KB Flash)

| 分区 | 起点 | 大小 | 用途 | 约束 |
|---|---|---|---|---|
| **BOOT** | 0x0800_0000 | 32 KB | bootloader(上电首跑;校验/跳转/升级搬运) | 出厂后只读;RDP + WRP 保护 |
| **APP** | 0x0800_8000 | 472 KB | 应用镜像(当前种子工程落此) | 头部带 image header(magic/版本/长度/CRC32);VTOR 重定位到 0x0800_8000 |
| **PARAMS** | 0x0807_F000 | 4 KB | 标定参数/运行计数(J2 标定数据落点) | 独立擦除页;写入带 CRC+双副本 |

- **双 bank OTA 路线**:G474 512KB 器件支持 dual-bank 模式(bank 交换启动)——A/B 镜像各占一 bank,升级写备 bank、校验后切换,失败自动回滚。**PROVENANCE: PLACEHOLDER——DBANK 选项字节与 bank 切换细节以 ST RM0440 为准,G4 卡开工首日读手册固化**;在此之前单 bank+BOOT 搬运方案为保底。
- **安全启动占位**:BOOT 校验 APP 的 CRC/签名(正式版换签名验证);RDP Level 1 起步(防读出/防克隆,C3 review),量产升 Level 2 前过方向门禁(不可逆)。
- **回滚语义**:新镜像首启动须在看门狗窗口内自证(置 confirmed 标志),否则 BOOT 回退旧镜像——与「被测不能自证」一致:镜像自己说好不算,要活过看门狗才算。

## 对现有工程的立即改动(本轮已做)

链接脚本 `bsp/nucleo_g474/stm32g474re.ld`:FLASH 区改为 APP 分区(origin 0x08008000/472K),
新增 BOOT/PARAMS 区注释占位;gate cross-compile 腿新增分区存在性断言。种子仍无真 bootloader
(BOOT 区空置),烧录调试时可临时用全片布局——**但仓库内布局以本分区表为准**。

## 证伪条件与回退

- 若产品线确认永不 OTA/联网:APP 可回收 BOOT 32K 中的 28K(留 4K 最小引导),回退成本=改一行 origin;
- 若 RM0440 阅读后 dual-bank 粒度与假设不符:A/B 改为 BOOT 搬运式单 bank 方案,分区表不变;
- 反向(现在不留、将来补):链接脚本/镜像头/烧录脚本/产测全链返工——这正是现在占位的理由。

## 待人签字(方向门禁)

- [x] 分区表与 32K/4K 预留量认可 —— ✅ 方案一签字(2026-07-08)
- [x] RDP 策略 L1 起步 —— ✅ 方案一签字(2026-07-08);L2 时点后议
