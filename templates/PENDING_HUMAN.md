# PENDING_HUMAN — 等人签字/回答(loop 不等待,但每轮报告)

> 回答方式:直接在任何会话里说,当值 AI 会更新对应卡状态并解除阻塞。
> loop 不会停下来等这些——它跳过阻塞卡做别的,每轮在 DIGEST 里持续报告卡点未解除。

## 方向 gate(战略选择,答案会改变要做什么)
1. **<决策名>**:<背景一句话> → <选项 A / 选项 B / 另选?> — 建议:<你的推荐 + 理由>

## 数据 gate(PII / 非本项目资源 / 不可逆)
2. **<资源/操作>**:<为什么需要人批> — 机器可查:<denylist 命中 / 需逐一具名确认>

## 对外 gate(发送 / 发布 / 花钱 / 生产切换)
3. **<对外动作>**:<一句话> — 现状:结构上人审前不可执行

## 事实核实(决定哪些牌能打)
4. **<需要确认的前提>**:<问题> — 口头答即可,当值 AI 落卡

---
每条写清:**触发条件、需要人给什么、答案解锁什么**。让签字变成 10 秒的事。
