# 产品功能定义 → 核心参数定义(P14 引用纪律,每条溯源)

> 结构化真源:m0/functions.json(由 leg_functions 校验一致性)。本文是人读叙述。
> **两类溯源(P14)**:🧬**推演类**给推导链+第一性依据(N1-N6,可推翻);
> 📎**引用类**给出处+**批判分析**([有据])。
> 功能不是拍脑袋列的——每条要么从具身大脑第一性推出,要么参考已验证产品并批判其局限。

---

## 零、推导起点:具身大脑的第一性(data-product-spec 第一层)

大脑学条件分布 **π(动作 | 观察, 语言, 状态)**。由此推出六条数据要求(推演,非引用):

| | 第一性要求 | 推导 |
|---|---|---|
| **N1** | 观察-动作时间对齐 | 错位=给对的观察配错动作=标签噪声,直接毒化训练信号 |
| **N2** | 分布覆盖 | 模型只泛化到训练分布邻域,多样性是泛化唯一来源 |
| **N3** | 动作可执行 | 学到的动作若在目标本体构型空间外,部署物理不可行 |
| **N4** | 演示一致性 | 一致性=策略性能天花板(混合质量比小而一致更差)[有据 route-02] |
| **N5** | 观察部署同构 | 训练/部署视角不同→输入分布偏移 |
| **N6** | 接触物理来自真机 | 仿真成本1%但接触力/触觉/形变是其结构盲区 [有据 route-04 sim2real] |

> 产品的每个功能,下面都标它由哪条 N 推出、或参考了哪个已验证产品。

---

## 一、产品功能定义(10 项,每项溯源)

### 🧬 从第一性推演的功能

**F1 · 记录末端位姿轨迹** — 推演自 **N1**
把人手演示的末端 6-DoF 位姿+夹爪宽度转录为动作标签。这是无本体采集的核心动作,
UMI 范式的存在理由(把人手当机器人)。
- 📎 参考:UMI(arXiv 2402.10329,无本体转录范式)
- ⚠ 批判:EE-pose 与目标本体可达空间无关,引入约 32.5% IK 不可行(route-04)→ 需 F5 缓解

**F2 · 记录接触力** — 推演自 **N6**
与视觉/位姿同步采集每指接触力。这是本产品的差异化核心:仿真够不到的模态。
- 📎 参考:sim2real gap(route-04 来源 2605.28812 等);UMI-FT(2601.09988)
- ⚠ 批判:手持自由运动只能**牛顿级非计量级**(route-02 已订正措辞);**跨本体力迁移零生产级证据**(M2b 硬缺口)——这是产品最大风险

**F3 · 记录腕视角视觉** — 推演自 **N5**
腕装鱼眼,与机器人部署腕相机同构,跨本体几乎不变。
- 📎 参考:UMI(2402.10329,腕视角跨本体一致性最高)
- ⚠ 批判:鱼眼畸变对 VLM 是 OOD;卷帘快门快速运动产生果冻畸变污染 VIO(route-03/04)

**F4 · 多模态时间同步** — 推演自 **N1**
视觉/位姿/力/夹爪统一时基,同步误差可测量并写入证书。
- 📎 参考:route-03 同步调查(PTP vs NTP);π0 频率要求
- ⚠ 批判:软件时间戳只到 ms 级;亚毫秒须硬件触发+全局快门,**与消费相机/低成本直接冲突**;π0"亚毫秒"查无官方出处(route-03 已订正)

### 📎 参考已验证产品的功能

**F5 · 采集时可行性反馈** — 参考 FeasibleCap,服务 **N3**
采集每帧对目标 URDF 做可达+自碰撞检查,当场提示不可行动作,把 32.5% 废弃前移到源头。
- 📎 出处:FeasibleCap(arXiv 2603.07580,60Hz 纯解析、无头显)
- ⚠ 批判:只针对给定 URDF,换客户本体需重算;不解决软硬顺应的力学 gap

**F6 · 采集时实时质检** — 参考质控滞后痛点,服务 **N4**
机上实时算 tremble/丢帧/静止/可行性,当场提示重采,把废数据拦在源头。
- 📎 出处:采集运营调查(route-02,鹿明有效率仅 70%,CTO 亲证"问题在采集时注定")
- ⚠ 批判:穹彻"边采边筛"已部分占位;须讲清"证书(可复算规格)"与"筛选"的差异

**F7 · 原生训练格式输出** — 参考 LeRobot 事实标准,服务 **N2**
原生输出 LeRobot v3,官方 loader 直读,含自然语言任务标注。
- 📎 出处:LeRobot(route-04;GR00T 训练管线原生用 LeRobot 格式)
- ⚠ 批判:PI 承认大规模训练仍用 RLDS("LeRobot 可扩展性不足")→ 须同时提供 RLDS 导出

**F8 · 质量证书交付** — 参考行业证书空位,服务 **N4**
每批数据随附十项可复算质量证书。
- 📎 出处:行业无可审计证书(route-04);工信部《具身智能数据采集规范》行标制定中
- ⚠ 批判:证书认证**形式合规**,认证不了**训练价值**(买方真正的验收器是自己的模型;route-03 采购)→ 须配训练回路抽样验证

**F9 · 握力解耦** — 参考 TacUMI,服务 **N6**(力测量保真)
可锁定夹爪机构,锁定后人手握力反作用由刚性结构承载,退出力测量。
- 📎 出处:TacUMI(arXiv 2601.14550,rack-pinion 机械自锁)
- ⚠ 批判:只在静态锁定相位有效;连续变抓持时握力污染回归(route-02 未解 6)

**F10 · 跨设备标定一致** — 参考混池要求,服务 **N2**(规模化)
出厂自动标定,同批设备内外参偏差有量化上界,标定档随数据交付。
- 📎 出处:跨设备混池要求(route-04;不一致则规模化归零)
- ⚠ 批判:单人硬件最常见死法(第 30 台起数据混不了池);是**制造问题非设计问题**

---

## 二、核心参数定义(由功能推出,每个带值+来源+批判)

| 参数 | 值 | 服务功能 | 来源 | ⚠ 批判/边界 |
|---|---|---|---|---|
| **P1 位姿采样率** | 200 Hz | F1 | IMU 融合自然率,给重采样余量(π0 需≥50Hz) | 交付降采样到 50/30Hz;**升采样=编造** |
| **P2 位姿精度** | ≤2 mm | F1 | 操作任务需求 | 亚毫米对喂 VLA 过配;精度让位傻瓜化 |
| **P3 位姿漂移** | ≤5 mm/ep | F1 | 板载 VIO 闭环<1%,单条数十秒可接受 | 超标 episode 证书标红 |
| **P4 每指力率** | ≥500 Hz | F2 | 冲击瞬态毫秒级,50Hz 只见"碰了" | CoinFT 实际带宽公开无,软硅胶低通存疑 |
| **P5 力量程** | ±50 N | F2 | 覆盖家用/轻工 | 力标签**牛顿级非计量级**(已订正) |
| **P6 腕相机视场** | 155° | F3 | UMI 验证的部署同构视角 | 鱼眼畸变对 VLM 是 OOD |
| **P7 视觉帧率** | 60 fps | F3 | 采集侧为 VIO/力对齐 | 喂 VLA 主流 30fps(LeRobot 72%),**交付降采样,高帧非为喂模型** |
| **P8 视觉分辨率** | 1280px(采集侧) | F3 | 采集侧 VIO 余量 | 交付 224²/640×480;对喂 VLA 过配 |
| **P9 同步误差** | ≤5ms P99(铺量)/ ≤1ms(旗舰) | F4 | 软件戳可达,对 50Hz 够 | 旗舰须换全局快门相机;与 cert 阈值一致 |
| **P10 可行性检查率** | 60 Hz | F5 | FeasibleCap 60Hz 在线 IK | 针对给定 URDF |
| **P11 整机重量** | ≤800g(硬上限 1kg) | F6 | 疲劳→演示变形→一致性↓(N4) | **重量是数据质量问题非舒适问题** |
| **P12 可靠性 MTBF** | ≥200 采集小时 | F6 | 一线"一天可用不足六小时" | 被低估的杀手,需样机实测 |
| **P13 证书指标数** | 10 项 | F8 | m0/cert.py 已实现 | 认证形式合规非训练价值 |

> 参数否决线(破一条产品不成立,承 data-product-spec):同步>5ms、重量>1kg、
> 同批标定不一致、力通道被握力污染、力无牛顿级标定。

---

## 三、这份定义怎么被机器守住

- **结构化真源** m0/functions.json:功能与参数的机器可读定义。
- **leg_functions** 校验:①每个功能有 basis(N 标签)或 source;②参考类必带 critique(P14);
  ③每个参数 traces_to 一个真实功能(无孤儿参数);④参数值不与 cert 阈值/bom 矛盾。
- 改功能或参数而不更新溯源/批判 → gate 红。**功能定义不是一次性文档,是被验证器守住的活规格。**

---

*P14 声明:🧬推演项标第一性依据(N1-N6),可推翻;📎引用项带出处([有据])与批判。
所有 [有据] 可追溯到 practice/research/route-0*.md 的带链接来源。*
