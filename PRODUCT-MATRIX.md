# 产品矩阵与 Roadmap:三档机型 × 四模态选型

> 由 m0/tradeoff.py 决策矩阵输出(七维加权,可复算,过 gate)。每个选型可追溯到
> practice/routes/*.json 的评分与 practice/research/route-0*.md 的原理与出处。
> 权重档位见 tradeoff.py PROFILES。**改权重不改数据**——排名是算出来的,不是拍的。

---

## 一、矩阵引擎选出的各模态冠军(按产品档)

| 模态 | 旗舰(精度/稀缺优先) | 铺量(成本/轻便优先) | 灵巧手专用(多模态优先) |
|---|---|---|---|
| **触觉** | 电容PaXini / 磁AnySkin(并列0.846) | **磁AnySkin(0.877)** | 电容PaXini / 磁AnySkin(并列0.835) |
| **力测量** | **每指CoinFT(0.769)** | 每指CoinFT(0.769) | 每指CoinFT(0.774) |
| **位姿** | 实时板载VIO(0.715) | **实时板载VIO(0.785)** | 实时板载VIO(0.687) |
| **同步** | **硬件触发MCU(0.785)** | **软件时间戳(0.877)** | 硬件触发MCU(0.809) |

**三个可复算的硬结论:**
1. **每指 CoinFT 和 实时板载 VIO 在所有档位都赢**——这两条是无争议选型,不管做哪档机型都用它们。第一性原理("测量点越贴接触越好"、"傻瓜化=板载增量VIO")在数字上被证实。
2. **触觉和同步是分档的分界线**:
   - 触觉:旗舰用电容(计量/稀缺)或磁(一致),铺量明确用磁 AnySkin(成本+一致性双高)。**视触觉在所有档都排中游**——虽有 UMI 落地先例,但 sim2real 易造(稀缺价值低)拖累了它。
   - 同步:旗舰用硬件触发(必须换全局快门相机),铺量用软件时间戳(保 GoPro,认 ms 级)。**这正是旗舰 vs 铺量的物理分界。**
3. 视触觉的"落地先例"优势没赢过它的"仿真易造→不稀缺"劣势——**如果产品卖点是稀缺性,视触觉是错的选择。**

---

## 二、三档产品定义

### 🅰 旗舰机 "计量档"(Contact-Pro)
**目标客户**:做 contact-rich 任务(插装/装配/柔性物)的大脑创业公司,要最高保真力/触觉。
**选型**:每指 CoinFT 六轴力 + 电容 PaXini 六维力触觉(计量/稀缺) + 实时鱼眼板载 VIO + **硬件触发 MCU 同步(全局快门工业相机)** + 可锁定夹爪(去握力)。
**关键取舍**:为亚毫秒同步与计量级触觉,放弃 GoPro、上全局快门工业相机 → BOM 高、重、不傻瓜。
**BOM 估**:¥12,000–18,000。**诚实标注**:力标签是"锁定相位可标定静态力 + 运动段牛顿级",非全程计量级(物理约束,见 route-02)。

### 🅱 铺量机 "众包档"(Contact-Go)
**目标客户**:需要规模+多样性的数据采集,众包/分布式,成本敏感。
**选型**:磁 AnySkin 触觉(免标定台间一致,换皮$5-10) + 每指 CoinFT(或降级电机电流估抓持力) + 实时鱼眼板载 VIO + **软件时间戳同步(保消费相机,认 ms 级)** + 一键开采。
**关键取舍**:认 ms 级同步(对 50Hz 动作足够,见 route-03 对 π0 亚毫秒的订正)、认牛顿级力,换来轻、便宜、可众包、傻瓜化。
**BOM 估**:¥3,000–5,000。**这是最可能规模化的一档。**

### 🅲 灵巧手专用 "外骨骼档"(Dexterous)
**目标客户**:训练特定灵巧手(Inspire/XHand)的客户,要关节级对齐+触觉。
**选型**:外骨骼关节级采集(DexUMI 路线,gap 最低) + 电容/磁触觉 + 每指 CoinFT + 硬件触发同步 + 视觉 inpainting 换手。
**关键取舍**:每类灵巧手一套外骨骼硬件,不通用,但对固定手 embodiment gap 近零。
**BOM 估**:¥15,000+,项目制而非标品。

---

## 三、跨档共用的架构不变量(不分档,都这么做)

来自 route-04 的"服务多本体"配方,与档位无关:
1. **动作标签**:相对 EE-pose+夹爪宽度通用空间采集 → FeasibleCap 60Hz 源头可行性约束 → 交付前 VISTA 物理校验+按客户 URDF 重定向。**把本体特异性从数据剥离。**
2. **视觉主输入**:腕装鱼眼 155° + Cloak mask 掉末端(一份数据服务夹爪+灵巧手+不同臂)。外部视角只作可选上下文。
3. **视觉规格**:训练交付 224²、采集 640×480@30fps(对齐 LeRobot 72% 主流);**高帧率/高分辨率只为 VIO 与力事件对齐服务,交付可降采样——不为喂模型过配。**
4. **每批交付质量证书**(m0/cert.py 已实现):十项指标可复算。

---

## 四、Roadmap(时间轴)

```
M0 (0-8周)   铺量档 Contact-Go 样机:CoinFT + AnySkin + 板载VIO + 软件同步 + 数据管线
             理由:最便宜验证全链路,BOM最低,最快出可训数据
             ↓
M1 (8-12周)  数据管线+证书打通(m0/已骨架就绪);跑通LeRobot官方loader
             ↓
M2 (并行,不阻塞硬件) ★最致命验证:跨本体力迁移复现实验(公开UMI-FT/ForceMimic数据)
             结论支持→力觉押注成立,继续;否决→退回纯质检UMI
             ↓
M3 (+4周)    工业contact-rich任务首个pilot(分拣/插装),North-star首测
             ↓
M4           成立则向旗舰档Contact-Pro延伸(计量触觉+硬件同步);
             灵巧手需求出现则做Dexterous项目制
```

**排序逻辑**:先做最便宜、最快出数据的铺量档验证商业闭环;旗舰档等力觉价值被 M2/M3 证实再上(避免为未验证的计量级需求先付高 BOM)。

---

## 五、这张矩阵回答了用户的原问题

- **"触觉有几条技术路线"** → 8 条,route-01 逐条第一性+对比矩阵;冠军按档不同(旗舰电容/铺量磁性)
- **"其他方向一样"** → 力测量 9 条(route-02)、位姿 7 条+同步 4 条(route-03)、动作标签 5 条+视觉 5 条(route-04)
- **"分别判断价值"** → tradeoff.py 七维加权可复算得分,不是形容词
- **"从精度/多模态/轻便/成本各维度形成竞争力"** → 七维正是这些,三档 profile 就是"不同维度优先"的产品化
- **"产品定义/roadmap/矩阵"** → 本文三档机型 × 四模态选型 + 五段 roadmap

**一句话**:不是"哪个传感器最好",是**"旗舰用计量电容力觉打稀缺性、铺量用磁性AnySkin打成本一致性、灵巧手用外骨骼打关节级"**——同一套模态,三种权重,三个产品。而 CoinFT 每指力 + 板载 VIO 是三档共用的地基,因为它们在所有权重下都赢。
